# 📚 Подробное руководство по использованию

## Содержание
1. [Установка и настройка](#установка-и-настройка)
2. [Базовое использование](#базовое-использование)
3. [Расширенные возможности](#расширенные-возможности)
4. [Интерпретация результатов](#интерпретация-результатов)
5. [Интеграция с учебным процессом](#интеграция-с-учебным-процессом)
6. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

## Установка и настройка

### Требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/KekCheburekLegend/code-quality-assessment-tool.git
cd code-quality-assessment-tool
Шаг 2: Создание виртуального окружения
```
```bash
# Для Linux/Mac
python -m venv venv
source venv/bin/activate

# Для Windows
python -m venv venv
venv\Scripts\activate
```
### Шаг 3: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 4: Проверка установки
```bash
python scripts/run_analysis.py --help
```
### Базовое использование
#### Анализ одного файла
```bash
python scripts/run_analysis.py путь/к/вашему/файлу.py
```
#### Анализ директории
```bash
python scripts/run_analysis.py путь/к/директории --output результаты/анализ
```
#### Анализ с пользовательской конфигурацией
```bash
python scripts/run_analysis.py студенческие_работы/ \
  --output отчеты/семестр1 \
  --config моя_конфигурация.json
```
### Расширенные возможности
#### Программный API
#### Использование в ваших скриптах

```python
from The_project.src.analyzer import CodeAnalyzer
from The_project.src import ReportGenerator

# Создание анализатора
analyzer = CodeAnalyzer()

# Анализ файла
metrics = analyzer.analyze_file("пример.py")
print(f"Оценка: {metrics.score}/100")
print(f"Сложность: {metrics.avg_complexity}")

# Анализ директории
results = analyzer.analyze_directory("проект/")

# Генерация отчета
analyzer.generate_report(results, "отчет")

# Генерация рекомендаций
reporter = ReportGenerator()
recommendations = reporter.generate_recommendations(metrics.__dict__)
for rec in recommendations:
    print(f"- {rec}")
``` 
#### Кастомизация весов метрик
```python
analyzer = CodeAnalyzer()

# Настройка весов по вашему усмотрению
analyzer.metrics_weights = {
    'pep8': 0.30,          # Больше веса PEP8
    'complexity': 0.20,    # Меньше веса сложности
    'maintainability': 0.25,
    'docstrings': 0.15,
    'comments': 0.10
}

# Теперь анализ будет использовать новые веса
metrics = analyzer.analyze_file("код.py")
```